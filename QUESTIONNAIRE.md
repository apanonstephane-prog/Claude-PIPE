# Questionnaire Production — Claude-PIPE

> À remplir au début de chaque projet avant toute génération.
> Ce fichier est la mémoire de projet de Claude Code.

---

## 1. PROJET

- **Type** : `clip musical` / `pub` / `teaser` / `photo` / `portfolio` / `générique` / `autre`
- **Titre / Nom du projet** :
- **Client / Artiste** :
- **Deadline** :

---

## 2. FORMAT & DURÉE

- **Durée** : (ex: 15s, 30s, 1min, 3min...)
- **Format** : `9:16 vertical` / `16:9 horizontal` / `1:1 carré`
- **Plateforme cible** : `Instagram Reels` / `TikTok` / `YouTube` / `Cinéma` / `Web`
- **Résolution** : `1080p` / `4K`

---

## 3. CONTENU & RÉFÉRENCES

- **Photos client disponibles (CREF)** : `oui` / `non` — combien ?
- **Musique / son** : `oui` / `non` — titre / artiste / BPM ?
- **Références visuelles** : (URLs, films, clips de référence)
- **Textes / sous-titres** :

---

## 4. STYLE VISUEL

- **Ambiance / Mood** : (ex: sombre, épique, lumineux, romantique, futuriste...)
- **Palette de couleurs** : (ex: noir & or, bleu froid, chaud & saturé...)
- **Style** : `cinematic` / `photorealistic` / `documentary` / `abstract` / `poster` / `illustration` / `logo`
- **Modèle image préféré** : `nano-banana-pro` / `flux-dev` / `flux-schnell` / `auto`
- **Modèle vidéo préféré** : `kling-3.0` / `kling-v2.1-pro` / `ltx-video` / `auto`

---

## 5. MONTAGE & RYTHME

- **BPM** : (si musique)
- **Rythme de coupe souhaité** : (ex: noire noire croche croche blanche — ou libre)
- **Types de plans** : `gros plan` / `plan moyen` / `plan large` / `plan de coupe` / `insert` / `aérien`
- **Transitions** : `fade` / `cut sec` / `wipe` / `zoom` / `mixte`
- **Effets spéciaux** : (slow motion, accéléré, grain film, flare...)

---

## 6. CONTRAINTES

- **Budget Replicate approximatif** :
- **Éléments à éviter** :
- **Éléments obligatoires** :
- **Notes libres** :

---

*Ce questionnaire est lu automatiquement par Claude Code au début de chaque session de production.*
